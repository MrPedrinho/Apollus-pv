const {removeBan} = require("../assets");
module.exports = {
    help: "Tira a ban de usar o bot",
    async execute (message, props) {
        const success = await removeBan(message)
        if (success.ok) {
            message.reply("Okok")
        } else {
            message.reply(success.error)
        }
    }
}